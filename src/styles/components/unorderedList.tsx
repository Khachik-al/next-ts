import type { ComponentStyleConfig } from '@chakra-ui/theme'

export const UnorderedList: ComponentStyleConfig = {
  variants: {
    contentList: {
      pt: 6,
      borderTop: '1px solid',
      borderColor: 'gray.200',
    },
  },
}
